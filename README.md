# azizulwahid.com

Personal site for Azizul Wahid.

Built with Next.js static export and deployed to GitHub Pages.

```bash
npm ci
npm run dev
npm run build
```

The GitHub Pages workflow deploys the generated `out/` directory from `master`.
